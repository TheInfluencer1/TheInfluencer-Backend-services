const router = require("express").Router();
const  {
    AUTH_ERRORS,
    USER_ERRORS,
    OTP_ERRORS,
    SERVER_ERRORS,
    SUCCESS_MESSAGES,
} = require('../../../../config/const');

// Route to view all received reviews for the influencer (GET /api/influencer/reviews)

// Route to respond to a review (POST /api/influencer/reviews/respond/:id)

// Route to request removal of a fake or inappropriate review (DELETE /api/influencer/reviews/:id)
