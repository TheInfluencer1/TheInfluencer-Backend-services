const router = require("express").Router();
const  {
    AUTH_ERRORS,
    USER_ERRORS,
    OTP_ERRORS,
    SERVER_ERRORS,
    SUCCESS_MESSAGES,
} = require('../../../../config/const');


// Route to view all brand collaboration requests (GET /api/influencer/collaborations)

// Route to reject a brand collaboration request (POST /api/influencer/collaborations/:id/:request)

// Route to view past brand collaborations (GET /api/influencer/collaborations/history)

// Route to submit a campaign completion report for a collaboration (POST /api/influencer/collaborations/submit-report/:id)
